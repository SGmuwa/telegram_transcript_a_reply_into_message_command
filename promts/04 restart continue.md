Ты — профессиональный python разработчик с глубокими знаниями telethon, linux, whisper, asyncio и многопоточности.

Прошу тебя выполнить три задания (если кратко):

1. Продолжение после перезапуска
2. Отменять транскрипцию если сообщение уже нельзя обработать
3. Улучшать модель для старых сообщений

Подробнее:

# Продолжение после перезапуска

Твоя задача реализовать, чтобы при старте docker-compose или при старте сервиса telegram_transcript_a_reply_into_message_command находил в истории сообщений все сообщения, которые:

1. Либо не до конца обработаны.
2. Либо обработаны моделью ниже, чем DEFAULT модель.

Сейчас можно заметить много сообщений вида:

> /transcription 🤖 Транскрипция: Скачивание медиа 0%
> Дата завершения скачивания: —

Я бы хотел, чтобы при старте сервис мог такие сообщения находить и обрабатывать.

Я предлагаю завязаться на словосочетания в начале, в том числе команды, которые ещё не успели обработаться:

1. "/transcription"
2. "/tr"
3. "/ts"

При старте работы если будет большое количество необработанных сообщений, то будет блокировка или заморозка от Telegram. Поэтому при старте работы не стоит отправлять сообщения с высоким приоритетом. Однако в ходе работы можно отправлять сообщения с высоким приоритетом, например при завершении задания.

Сервис должен уметь параллельно обрабатывать как сообщения для обработки на старте, так и сообщения, которые получены в ходе работы сервиса. То есть не должно быть такого, что программа зависнет на обработке старых сообщений.

Не стоит обрабатывать старые сообщения, если их возраст более недели, даже если они начинаются со слова "/transcription 🤖 Транскрипция"

К сожалению в ходе обработки сообщений на старте потеряется информация о том, какая нужна модель и какой язык и так далее. Поэтому бери значения по-умолчанию.

Нельзя ход работы сохранять на диск, потому что я планирую включать сервис то на сервере, то на ноутбуке. Сервер у меня слабый, а ноутбук — мощный. Я хочу на ноутбуке включать обработку незавершённых сообщений.

# Отменять транскрипцию если сообщение уже нельзя обработать

Стоит отметить, что сообщение может быть недоступно для редактирования. В таком случае тебе не нужно его обрабатывать, так как всё равно не получится его изменить. Просто проверь, что если получил Exception как и для высокого приоритета, так и для низкого приоритета, то сообщение не будет далее обрабатываться далее. Самый частый пример: в личных сообщениях если сообщению более двух суток

# Улучшать модель для старых сообщений

Я бы хотел при старте мощного ноутбука чтобы он брал старые сообщения и улучшал качество транскрипции. Поэтому предлагаю, чтобы при старте сервиса сервис брал старые сообщения и если качество хуже, чем default в переменных окружения, то брал бы в работу такое сообщение и редактировал его с default моделью. Если качество модели default равно или ниже, чем в сообщении, то не трогал бы его.

Во время старта сервиса необходимо находить ЗАВЕРШЁННЫЕ сообщения с текстом "🤖 Транскрипция:" или "🤖 Транскрипция (model tiny):", возраст которых не более недели. Вот приоритет моделей:

1. tiny — самая низкая точность
2. base
3. small
4. medium
5. turbo
6. large — самая высокая точность

Во время процесса улучшения качества сообщений их нельзя редактировать для промежуточного результата. Их можно редактировать только в самом конце, когда улучшенная транскрипция готова.

Если сообщение сделано старой версией сервиса где подпись "🤖 Транскрипция:", то считать, что используемая модель: `small`.

