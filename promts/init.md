Напиши пожалуйста на Telethon клиента, который отлавливает отправленные (а не полученные) сообщения. Если это команда /tr или /transcription, то скачать аудио из ответа (reply), выполнить транскрипцию с помощью from faster_whisper import WhisperModel и отредактировать сообщение: вместо всего текста с командой /tr или /transcription надо заменить на транскрипцию.

Программу назовём telegram_transcript_a_reply_into_message_command

Необходимо создать и python программу, и dockerfile, и docker-compose файл, и пример секретов.

Для временных файлов должен быть специальный temporary mount. В ходе работы python программы временные файлы должны удаляться.
Для скаченных моделей должен быть volume mount. Он должен сохраняться после перезапуска docker-compose и позволяет не скачивать заново модели whisper например large или tiny.

Параметры:

DEFAULT_MODEL_NAME = "large" (но у пользователя должна быть возможность передавать этот параметр, например /tr model=large или /tr model=turbo)
DEFAULT_LANG = "ru" (но у пользователя должна быть возможность передавать этот параметр, например /tr lang=en или /tr lang=ru,en)

Любая операция на редактирование сообщения с низким приоритетом не должна быть чаще, чем раз в две минуты, а то получим бан от Telegram. Например если надо обновлять прогресс по двум сообщениям сразу, то сначала спустя две минуты отправляется запрос на обновление одного сообщения, а потом спустя две минуты другого сообщения. При высоком приоритете можно редактировать сообщение незамедлительно.

Допустим я, как пользователь:

1. Включаю программу
2. Программа просит заполнить файл по примеру и закрывается
3. Я заполняю файл для входа в аккаунт @SG_Muwa
4. Заново запускаю программу
5. Программа работает, входит в мой Telegram аккаунт, пишет, что ждёт новых сообщений

6. Я захожу в Telegram Desktop под тем же аккаунтом @SG_Muwa
7. Мне присылают сообщения, я тоже отправляю сообщения. Может даже аудиосообщения. Программа не реагирует.
8. Я захотел транскрипцию сообщения. Поэтому я в ответ на аудиосообщение или в ответ на видеосообщения или в ответ на музыку или в ответ на видео отправляю команду /tr или /transcription
9. Бот редактирует сообщение с высоким приоритетом на

/transcription 🤖 Транскрипция: Скачивание медиа 0%
Дата завершения скачивания: 2026-02-13 10:00:00 +04:00

Бот редактирует сообщение с низким приоритетом каждые две минуты до тех пор, пока сообщение не будет скачано.

10. После завершения скачивания аудио сообщение редактируется с низким приоритетом на

/transcription 🤖 Транскрипция: Конвертация медиа 0%
Дата завершения конвектирования: 2026-02-13 10:12:34 +04:00

Бот редактирует сообщение с низким приоритетом каждые две минуты до тех пор, пока медиа не будет сконвертировано в нужный формат (wav?). Если библиотека конвертации не поддерживает progress bar, то редактируй тогда на такое сообщение:

/transcription 🤖 Транскрипция: Конвертация медиа (прогресс неизвестен)

11. После завершения конвертации сообщение редактируется с низким приоритетом на

/transcription 🤖 Транскрипция: Извлечение текста 0%
Дата завершения: 2026-02-13 10:13:45 +04:00

Бот редактирует сообщение с низким приоритетом каждые две минуты до тех пор, пока весь текст не будет получен из аудио.

12. После завершения извлечения текста всё сообщение с высоким приоритетом заменяется на транскрипцию. Формат сообщения:

🤖 Транскрипция:
> Полученный текст из аудио

То есть сначала идёт emoji, затем "Транскрипция:\n", затем полученный текст из аудио в формате цитаты.

Если итоговое сообщение получается больше, чем поддерживает Telegram, то отправляемое сообщение имеет другой формат:

🤖 Транскрипция прикреплена файлом

И в редактируемом сообщении прикрепляется файл.

Если в ходе работы появляется какая-либо ошибка, то обработка сообщения прерывается и с высоким приоритетом сообщение редактируется на следующее:

🤖 Транскрипция провалена из-за ошибки:
```
Текст ошибки
```

